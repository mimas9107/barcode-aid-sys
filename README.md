# barcode aid sys
##
## issue
手機開啟後看到 “Error: getUserMedia is not defined”，表示瀏覽器 不允許使用相機 API。這種問題非常常見，通常不代表程式寫錯，而是環境條件不符合。
使用的是 http://（非 HTTPS）
👉 行動裝置瀏覽器（Chrome / Safari / Firefox）不允許在 HTTP 的網頁使用相機
👉 只有 HTTPS 或 localhost 可以使用 getUserMedia()
| 網址                                                 | 相機權限    |
| -------------------------------------------------- | ------- |
| [http://192.168.x.x:5000](http://192.168.x.x:5000) | ❌ 相機被禁止 |
| [http://10.0.x.x:5000](http://10.0.x.x:5000)       | ❌ 相機被禁止 |
| [http://servername:5000](http://servername:5000)   | ❌ 相機被禁止 |
| [http://localhost:5000](http://localhost:5000)     | ✔ 允許    |
| [https://你公司的域名](https://你公司的域名)                   | ✔ 允許    |

方案 A：讓 Flask 自帶 HTTPS（最簡單、立即可測）

你可以讓 Flask 临时加载自签名 SSL 凭证：

在 Windows 產生 SSL 憑證（一次即可）：
'''bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -nodes -days 365
'''
會產生：

cert.pem

key.pem

Flask 啟動加入 SSL context
'''bash
app.run(host="0.0.0.0", port=5000, ssl_context=('cert.pem', 'key.pem'))
'''
第一次手機會顯示「不受信任憑證」，點「允許」即可
（這是開發模式正常現象）

📌 此方法最快速、最直覺，不需要安裝任何額外東西。
